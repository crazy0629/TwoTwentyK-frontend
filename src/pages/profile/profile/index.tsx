import React from "react";
import { AppLayout } from "../../../layout/AppLayout";
import { ProfileSection } from "../../../modules";

export const ProfilePage: React.FC = () => {
  return (
    <AppLayout>
      <ProfileSection />
    </AppLayout>
  );
};
